---
const { url } = Astro.props;

--- 

<a href={url}>
    <slot/>
</a>

<style>

    a {
        padding: .5rem;
        border: 1px solid var(--color-digital-bg);
        border-radius: .25rem; 
    }

    a:hover {
        background-color: var(--color-digital-bg);
        color: var(--color-digital-content);
    }
    
</style>